services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: racao_db
      POSTGRES_USER: racao_user
      POSTGRES_PASSWORD: 123456
    ports:
      - "5432:5432"

  api:
    build: ./backend
    ports:
      - "8000:8000"
    depends_on:
      - db
